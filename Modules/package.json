{
  "name": "starter",
  "version": "1.0.0",
  "description": "Module - * module is a Reusable piece of code that encapsulates implementation details;          * usually a stand alone file, but it doesn't have to be.",
  "main": "ModulePattern.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rakshith",
  "license": "ISC",
  "dependencies": {
    "leaflet": "^1.9.3",
    "lodash-es": "^4.17.21"
  },
  "devDependencies": {
    "parcel": "^2.8.3"
  }
}
